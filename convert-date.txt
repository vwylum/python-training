def convertDate(date): # 106/11/11->20171111
    if len(date) != 9:
        print('wrong format')
        return date
    else:
        
        str1 = str(date)
        yearstr = XXX  # get year
        realyear = XXX + 1911  # chg year
        realdate = realyear + XXX + XXX  #combine
        return realdate


converted = convertDate('106/11/11')
print(converted)
                        
 
