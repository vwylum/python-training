def convertDate(date): # 106/11/11->20171111
    if len(date) != 9:
        print('wrong format')
        return date
    else:
        
        str1 = str(date)
        yearstr =  # get year
        realyear =  # chg year
        realdate = realyear +   #combine
        return realdate



converted = convertDate('106/11/11')
print(converted)
